package com.company;

public class Main {

    public static void main(String[] args) {

        // Math Class
        double p = Math.abs(5);
        double exp = Math.pow(2, 3);
        // exp holds the value of 8 (2 to the power of 3 is 8 / 2 * 2 * 2 is 8)
        double pie = Math.PI;

        //ceil is short for ceiling
        //Math.ceil(4.5) -----> 5
            // Opposite of Integer Truncation
        //Math.random
        int rd = (int)(Math.random() * 100);
        System.out.println(rd);

    }
}
